{
  "name": "dinuka-mckenzie-site",
  "version": "1.0.0",
  "scripts": {
    "dev": "tinacms dev -c \"bundle exec jekyll serve\"",
    "build": "tinacms build && bundle exec jekyll build"
  },
  "dependencies": {
    "@tinacms/cli": "^1.5.22",
    "tinacms": "^1.6.2"
  }
}
